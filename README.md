# React Portfolio (Parallax + 3D Road Experience)
Run:
```
npm i
npm run dev
```
- Replace `/public/portrait.svg` with your photo.
- Edit text in `src/components/*`.
- Accent color in `:root` of `src/styles.css`.
- Contact form wiring in `ContactSection.jsx`.
